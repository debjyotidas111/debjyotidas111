const arrow = (a,b)=> a+b; // arrow function  // when u have only single statement then arrow function can be used but when multiple statements are there, we have to use =>{}.

function normalFunction(c,d){  // normal function creation example.
    return c+d;
}
const normalFunction = function(e,f){  // function expression example.
    return e+f;
}